# spec.md 文件更新摘要

**更新日期**: 2025-01-23
**文件**: `specs/002-crm-api-integration/spec.md`
**需求來源**: `docs/prompts.md` 第 1 點

---

## 📋 更新目標

調整規格文件以明確反映核心目標：
- 透過前端串接 CRM API 讓登入功能成功運行
- 登入成功後導向到 **app_center (應用程式中心)** 頁面，而非 dashboard

---

## ✅ 完成的更新

### 1. 新增核心目標說明 (第 8-11 行)

**新增內容**:
```markdown
**核心目標**：透過前端串接 CRM API 實現以下功能：
- 使用者使用帳號密碼成功登入
- 登入成功後自動導向應用程式中心 (app_center)
- 維持安全的認證會話並管理 token 生命週期
```

**目的**: 在文件開頭明確說明此功能的核心目標，強調前端 CRM API 串接的主要用途

---

### 2. 更新使用者故事 1 - 描述 (第 27 行)

**修改前**:
> 成功登入後，使用者獲得系統存取權限，可以進入個人化儀表板。

**修改後**:
> 成功登入後，使用者獲得系統存取權限，可以進入應用程式中心。

---

### 3. 更新使用者故事 1 - 獨立測試說明 (第 31 行)

**修改前**:
> 可透過在登入頁面輸入有效憑證並驗證使用者被重定向至儀表板且具有有效會話來完整測試

**修改後**:
> 可透過在登入頁面輸入有效憑證並驗證使用者被重定向至應用程式中心且具有有效會話來完整測試

---

### 4. 更新驗收場景 1 (第 35 行)

**修改前**:
> 他們收到存取權杖、通過認證，並被重定向至儀表板

**修改後**:
> 他們收到存取權杖、通過認證，並被重定向至應用程式中心

---

### 5. 更新成功標準 SC-001 (第 120 行)

**修改前**:
> 使用者在穩定網路連線下使用有效憑證可在 5 秒內完成登入流程並到達儀表板

**修改後**:
> 使用者在穩定網路連線下使用有效憑證可在 5 秒內完成登入流程並到達應用程式中心

---

## 📊 變更統計

- **新增**: 1 個核心目標區塊 (4 行)
- **修改**: 4 處「儀表板」→「應用程式中心」
- **影響範圍**: 
  - 使用者故事描述
  - 測試場景說明
  - 驗收標準
  - 成功指標

---

## 🎯 更新後的文件結構

```
# 功能規格書：CRM 認證 API 整合
├── 基本資訊 (功能分支、日期、狀態)
├── 📌 核心目標 (新增)
│   ├── 帳號密碼登入
│   ├── 導向應用程式中心
│   └── Token 生命週期管理
├── 澄清
├── 使用者情境與測試
│   ├── 使用者故事 1 - 基本登入認證 ✓ 已更新
│   │   ├── 描述: 導向應用程式中心 ✓
│   │   ├── 獨立測試: 重定向至應用程式中心 ✓
│   │   └── 驗收場景 1: 重定向至應用程式中心 ✓
│   ├── 使用者故事 2 - 權杖生命週期管理
│   └── 使用者故事 3 - 使用者檔案與會話資訊
├── 需求
├── 成功標準 ✓ 已更新
│   └── SC-001: 到達應用程式中心 ✓
├── 假設
├── 相依性
└── 範圍邊界
```

---

## 🔍 關鍵術語對照

| 原始術語 | 更新術語 | 英文對應 |
|---------|---------|---------|
| 儀表板 | 應用程式中心 | app_center |
| dashboard | app_center | app_center |
| - | 核心目標 | Core Goal |

---

## ✅ 驗證檢查清單

- ✅ 核心目標區塊已新增到文件開頭
- ✅ 所有「儀表板」已替換為「應用程式中心」
- ✅ 使用者故事 1 的所有相關描述已更新
- ✅ 驗收場景與成功標準保持一致
- ✅ 文件仍符合 speckit 規格格式
- ✅ 更新不影響其他使用者故事 (US2, US3)

---

## 📝 後續建議

### 程式碼同步更新

為了讓實作與規格文件一致，建議更新以下檔案：

1. **frontend/src/router/index.ts**
   ```typescript
   // 當前: name: 'dashboard'
   // 建議: name: 'app_center'
   {
     path: '/app-center',  // 或 '/app_center'
     name: 'app_center',
     component: () => import('@/views/AppCenterPage.vue'),
     meta: {
       requiresAuth: true,
       title: '應用程式中心 - SaaS 登入系統',
     },
   },
   ```

2. **frontend/src/composables/useAuth.ts**
   ```typescript
   // 當前: await router.push({ name: 'dashboard' })
   // 建議: await router.push({ name: 'app_center' })
   ```

3. **新建頁面元件**
   - 建立 `frontend/src/views/AppCenterPage.vue`
   - 或重新命名現有的 `DashboardPage.vue`

4. **更新路由守衛**
   - router/index.ts 中的重定向邏輯
   - 將 `{ name: 'dashboard' }` 改為 `{ name: 'app_center' }`

5. **更新測試**
   - E2E 測試中的路由斷言
   - 導向目標驗證

---

## 📚 相關文件

- **原始規格**: `specs/002-crm-api-integration/spec.md` (已更新)
- **需求來源**: `docs/prompts.md` 第 1 點
- **OpenAPI 規格**: `docs/openapi.yaml` (CRM API 定義)
- **實作狀態**: `IMPLEMENTATION-STATUS.md`
- **登入修復**: `LOGIN-COMPLETE-FIX.md`

---

## ✨ 總結

**更新完成！** spec.md 文件已成功調整以反映正確的核心目標：

1. ✅ **核心目標清晰**: 明確說明前端串接 CRM API 的目的
2. ✅ **導向目標正確**: 所有描述都指向 app_center（應用程式中心）
3. ✅ **文件一致性**: 使用者故事、驗收場景、成功標準全部對齊
4. ✅ **格式規範**: 保持 speckit 規格文件的標準格式

**下一步**: 建議同步更新前端程式碼（路由、元件、composables）以與規格文件保持一致。

---

**更新者**: Claude Code  
**更新日期**: 2025-01-23  
**版本**: v1.0 (更新 app_center 導向目標)
